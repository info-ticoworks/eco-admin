'use strict';

/**
 * limpieza-por-unidad controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::limpieza-por-unidad.limpieza-por-unidad');
